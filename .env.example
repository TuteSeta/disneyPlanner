# PostgreSQL
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=disney_trip_service

# Anthropic — get your key at console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# API Gateway — Keys allowed to consume the backend (comma-separated)
API_KEYS=my-travel-front-dev
